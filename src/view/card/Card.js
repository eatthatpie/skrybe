import React from 'react';
import './Card.scss';

function Card(props) {
    return (
        <div
            className="card bg-light"
            contentEditable="true"
        />
    );
}

export default Card;